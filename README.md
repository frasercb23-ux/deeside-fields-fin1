# deeside-fields-fin1
cd ~/Desktop
mkdir deeside-fields-fin
cp deeside-fields-fin.html deeside-fields-fin/index.html
# copy your 3 images into the folder too, then:
cd deeside-fields-fin
git init
git add .
git commit -m "Initial site"
git branch -M main
git remote add origin https://github.com/YOURUSERNAME/deeside-fields-fin.git
git push -u origin main
